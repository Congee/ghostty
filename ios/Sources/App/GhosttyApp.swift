// GhosttyApp — iOS app entry point for the Ghostty remote terminal.

import SwiftUI

@main
struct GhosttyApp: App {
    var body: some Scene {
        WindowGroup {
            KineticRootView()
        }
    }
}
